/**
 * enum which holds all available types
 */
export const USER_TYPES_ENUM = {
  MAIN_GARAGE: "main_garage",
  SUB_GARAGE: "sub_garage",
  ADMIN: "admin",
  ADMIN_EMPLOYEE: "admin_employee",
};

/**
 * Get Logged User Type
 * @return string
 */
